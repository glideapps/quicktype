{
  "name": "quicktype",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "postinstall": "bower install && pulp build && pushd app && npm install && popd",
    "start": "cd app && npm start"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6"
  }
}
