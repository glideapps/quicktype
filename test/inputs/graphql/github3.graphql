query {
    viewer {
        login
        repository(name: "metapixel") {
            pullRequests(first: 10) {
                nodes {
                    state
                }
            }
        }
    }
}
