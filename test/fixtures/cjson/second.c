/* Second translation unit: including the generated header from more than
 * one .c file must compile and link (no duplicate symbol definitions). */

#include "TopLevel.h"

int quicktypeSecondTranslationUnit(void) {
    return 0;
}
